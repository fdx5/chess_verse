# Battle Chess Reforged — 개발 진행 가이드
# Development Handoff Guide (Opus → Feedback → Sonnet)

---

## 0. 파일 구성 / Files

| 파일 | 용도 | 사용 모델 |
|---|---|---|
| `00_HANDOFF_GUIDE.md` | 전체 절차 안내 (지금 이 문서) | — |
| `01_OPUS_ARCHITECTURE_PROMPT.md` | 분석 · 설계 지시문 | **Opus** |
| `02_SONNET_IMPLEMENTATION_PROMPT.md` | 구현 지시문 | **Sonnet** |

---

## 1. 전체 진행 순서 / The Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│ STEP 1  ─ Opus 세션                                          │
│  01_OPUS_ARCHITECTURE_PROMPT.md 의 PROMPT 블록 투입           │
│  산출: D1~D9 설계 문서 9종                                     │
│  ⏱ 예상: 1~2 세션 (분량이 크면 D1~D5 / D6~D9 로 분할)          │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ STEP 2  ─ 사용자 피드백 (§2 체크리스트 사용)                    │
│  · Open Decisions 항목 결정                                   │
│  · 누락/모호 지점 보완 요청 → 같은 Opus 세션에서 수정           │
│  · 확정본을 docs/design/ 에 파일로 저장                        │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ STEP 3  ─ Sonnet 세션 × N (스프린트 1개 = 세션 1개)            │
│  02_SONNET_IMPLEMENTATION_PROMPT.md 투입, {{SPRINT_N}} 치환   │
│  각 스프린트 종료마다 §3 수락 검사 후 다음 진행                  │
└──────────────────────────┬──────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ STEP 4  ─ 막히면 Opus 복귀                                    │
│  구조적 문제·설계 결함 발견 시 Sonnet에게 억지로 시키지 말고     │
│  Opus에게 해당 설계 문서 재설계를 요청 → 갱신본으로 Sonnet 재개  │
└─────────────────────────────────────────────────────────────┘
```

### 모델 분담 원칙 / Why this split

| Opus가 할 것 | Sonnet이 할 것 |
|---|---|
| 아키텍처 결정, 레이어 경계 | 결정된 구조대로 파일 작성 |
| 36개 전투 연출 시나리오 창작 | 시나리오를 클립 데이터로 변환 |
| AI 평가함수 설계·가중치 산정 | 탐색 알고리즘 코딩·최적화 |
| 성능 예산 수립, 리스크 분석 | 예산 충족을 위한 실측·튜닝 |
| 스프린트 분할, DoD 정의 | DoD 충족, 테스트 작성 |
| **막혔을 때의 재설계** | **반복적·기계적 확장 작업** |

> 핵심: **Sonnet에게 "무엇을 만들지 스스로 정하게" 하지 마십시오.** Sonnet은 명확한 명세가 주어질 때 가장 강하고, 모호한 설계 판단을 맡기면 방향이 흔들립니다. 반대로 36개 연출을 코드로 옮기는 것 같은 **반복 확장 작업은 Sonnet이 비용 대비 훨씬 효율적**입니다.

---

## 2. STEP 2 — Opus 산출물 피드백 체크리스트

Opus 결과를 받으면 아래를 대조하십시오. **✗ 가 하나라도 있으면 Sonnet으로 넘어가지 마십시오.**

### 2-1. 완결성 (누락 여부)

- [ ] D2에 **perft 기대값 표**가 실제 숫자로 있는가 (없으면 룰 버그를 잡을 방법이 없음)
- [ ] D5-3의 **전투 연출 36조합이 전부** 기술되었는가 — "나머지는 동일한 패턴으로" 같은 생략이 있으면 반려
- [ ] D4에 **유닛 12종의 치수·본 계층 이름**이 구체적으로 있는가
- [ ] D3에 **PST(Piece-Square Table) 실제 수치**와 평가 가중치가 있는가
- [ ] D6에 **모든 네트워크 메시지 타입**이 필드까지 정의되었는가
- [ ] D9-1 성능 예산표의 `?` 가 **전부 숫자로 채워졌는가**
- [ ] D9-2 스프린트 DoD가 **"검증 가능한 형태"** 인가 (예: "잘 동작함" ✗ / "perft depth 5 통과, 콘솔 에러 0" ✓)

### 2-2. 구조 건전성 (나중에 치명적인 것들)

- [ ] `chess-core` 가 Three.js에 의존하지 **않는** 구조인가 → 서버 재사용의 전제
- [ ] 전투 연출이 **데이터 레지스트리**로 등록되는 구조인가 → 요구사항 "지속적 업그레이드"의 전제
- [ ] `UnitProvider` 어댑터가 있어 **나중에 GLTF로 교체 가능**한가
- [ ] 연출 재생이 **게임 시계를 소비하지 않는다**는 규칙이 네트워크 설계에 반영되었는가
- [ ] AI가 **Web Worker** 실행으로 설계되었는가
- [ ] 모바일이 "나중에 대응"이 아니라 **품질 티어 시스템으로 처음부터** 설계되었는가

### 2-3. 결정해야 할 것 (Open Decisions)

Opus가 `⚠️ DECISION NEEDED` 로 표시한 항목들에 답을 주십시오. 통상 다음이 올라옵니다 — 미리 생각해두면 좋습니다:

| 예상 질문 | 판단 기준 |
|---|---|
| 오프닝북을 넣을 것인가 | 마스터 난이도의 초반 품질 vs 번들 크기. 넣는다면 경량 자체 포맷 권장 |
| 매치메이킹 레이팅(Elo) 도입 | 초기엔 불필요. 룸코드 친구 대전만으로 충분하다면 후순위 |
| 보드 표현(0x88 vs Bitboard) | 마스터 난이도 깊이를 얼마나 원하는지에 달림. 8+ depth를 원하면 Bitboard |
| 실시간 그림자 vs 베이크 | 모바일 성능 목표에 따라. 60fps 모바일이 우선이면 베이크 |
| 시계(체스 클럭) 프리셋 | 캐주얼 지향이면 무제한 기본 + 옵션 제공 |
| 서버 호스팅 형태 | 상시 구동 Node 필요 — 서버리스로는 WebSocket 유지가 어려움 |

### 2-4. 보완 요청 문구 예시

```
D5-3의 전투 연출이 12개만 작성되고 나머지는 생략되었습니다.
36조합 전부를 같은 형식으로 완성해 주십시오. 요약이나 "동일 패턴" 처리는 불가합니다.
분량이 크면 공격자별로 나눠서 6개 응답으로 주셔도 됩니다.

또한 D9-1 성능 예산표의 draw call / 삼각형 수 항목이 비어 있습니다.
Three.js 기준 근거와 함께 실제 수치로 채워 주십시오.
```

---

## 3. STEP 3 — 스프린트 운영 요령

### 3-1. 세션 분할 원칙
- **1 스프린트 = 1 세션.** 여러 스프린트를 한 세션에서 시키면 후반 품질이 급격히 떨어집니다.
- 스프린트가 크면 쪼개십시오. 특히 **전투 연출 스프린트는 공격자 유닛별로 6분할** 권장 (폰 6조합 → 나이트 6조합 → …).
- 새 세션은 항상 `docs/AGENT_RULES.md` + `docs/PROGRESS.md` + 해당 설계 문서만 읽고 시작.

### 3-2. 각 스프린트 수락 검사 (직접 실행)

```bash
npx tsc --noEmit           # 타입 에러 0
npm run lint
npm test
npm run build
grep -rn ": any" packages/*/src   # 새 any 유입 확인
grep -rn "TODO\|FIXME" packages/*/src
```

브라우저에서 열어 **콘솔 에러 0**, **이전 기능 회귀 없음**을 확인.

전투 연출 스프린트에서는 추가로:
```bash
git diff --stat HEAD~1 -- packages/client/src/anim/
# 새 연출 추가인데 엔진 코어 파일이 수정되었다면 레지스트리 설계 위반
```

### 3-3. 언제 Opus로 돌아가야 하는가

| 신호 | 조치 |
|---|---|
| 같은 버그를 Sonnet이 3번 이상 못 고침 | 설계 결함 가능성. Opus에게 해당 모듈 재설계 요청 |
| 성능 예산을 크게 못 맞춤 | Opus에게 렌더링 전략 재설계 요청 |
| 스프린트 간 인터페이스가 안 맞음 | 설계의 레이어 경계 문제. Opus에게 계약(contract) 재정의 요청 |
| `DEVIATIONS.md` 가 5건 이상 쌓임 | 설계와 현실이 벌어지는 중. Opus에게 설계 갱신 요청 |
| 스코프가 계속 커짐 | Opus에게 로드맵 재분할 요청 |

---

## 4. 프로젝트 문서 구조 (권장)

```
docs/
  AGENT_RULES.md        ← 02 프롬프트의 PROMPT 블록을 저장
  design/
    ARCHITECTURE.md            (D1)
    RULES_ENGINE.md            (D2)
    AI_DESIGN.md               (D3)
    UNITS_AND_ASSETS.md        (D4)
    ANIMATION_SYSTEM.md        (D5) ⭐
    NETWORK_DESIGN.md          (D6)
    UX_UI_SPEC.md              (D7)
    AUDIO_DESIGN.md            (D8)
    PERFORMANCE_AND_ROADMAP.md (D9)
  PROGRESS.md           ← 스프린트별 진행 기록 (Sonnet이 갱신)
  DEVIATIONS.md         ← 설계 대비 이탈 기록
  OPEN_QUESTIONS.md     ← 구현 중 발견된 모호 지점
```

---

## 5. 리스크 조기 경보 / Early Warnings

경험적으로 이런 프로젝트가 실패하는 지점은 거의 정해져 있습니다.

| 위험 | 언제 터지는가 | 예방 |
|---|---|---|
| 체스 룰 버그 (앙파상/캐슬링) | 온라인 대전 붙인 뒤 desync로 발견 → 재작업 폭발 | 스프린트 1에서 **perft 전부 통과**를 절대 조건으로 |
| 전투 연출이 하드코딩됨 | 연출 20개쯤 넣고 나서 구조 변경 불가 | 폴백 1개로 파이프라인 먼저 검증, 엔진 diff 감시 |
| 룰 엔진이 렌더러에 오염 | 서버 만들 때 재사용 불가 판명 | `chess-core` 의존성 0 규칙을 CI로 강제 |
| AI가 메인 스레드 블록 | 마스터 난이도 붙일 때 화면이 얼어붙음 | 처음부터 Worker. 절대 "나중에 옮기자" 금지 |
| 모바일 성능 붕괴 | 전부 만든 뒤 실기 테스트에서 10fps | 품질 티어를 스프린트 2에서 도입, 매 스프린트 저사양 티어 확인 |
| 연출 길이가 시계를 먹음 | 온라인 대전에서 불공정 시비 | 설계 단계에서 규칙 확정 (§D5-4) |
| 스코프 크리프 | 어디쯤에서 영원히 안 끝남 | 스프린트당 1세션, DoD 대조, 설계에 없는 기능 금지 |

---

## 6. 한 줄 요약 / TL;DR

**Opus에게는 "생각"을, Sonnet에게는 "노동"을 시키십시오.**
그 사이에 당신이 §2 체크리스트로 한 번 끊어주는 것이, 이 프로젝트가 완주하느냐 마느냐를 가릅니다.

Give Opus the **thinking**, give Sonnet the **labor**, and gate the handoff with the §2 checklist.
